export const FEATURE_PAID_ACCESS = true;
export const PAYMENT_MODE = 'mock'; // 'mock' or 'onchain'
export const SUPPORTED_TOKENS = ['USDT', 'USDC', 'SCOR'];
export const PLANS = {
  monthly: 30, // days
  quarterly: 90,
  yearly: 365,
};